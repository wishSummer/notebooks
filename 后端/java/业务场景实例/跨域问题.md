# 跨域问题

## 问题产生原因

### 协议不同

> `http://` 与 `https://`

### 域名不同

> 子域名不同 `example.com` : `api.example.com`
> 前缀不同 `www.example.com` : `example.com`
> 域名不同 `example.com` : `other.com`

### 端口不同

> `localhost:3000` : `localhost:1000`

### IP 地址与域名

> 服务器 IP `112.1.1.1` ： 服务器绑定域名 `example.com`